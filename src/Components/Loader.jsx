import { HashLoader } from "react-spinners";

const Loader = () => {
  return (
    <div className="flex min-h-[calc(100vh-349px)] items-center justify-center">
      <HashLoader color="#FF3811" />
    </div>
  );
};

export default Loader;
